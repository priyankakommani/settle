{
  "name": "@settle/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./http": "./src/http-status.ts",
    "./errors": "./src/error-codes.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no linter configured)\" && exit 0",
    "test": "echo \"(no tests)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
