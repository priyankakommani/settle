{
  "name": "@settle/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./http": "./dist/http-status.js",
    "./errors": "./dist/error-codes.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no linter configured)\" && exit 0",
    "test": "echo \"(no tests)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
